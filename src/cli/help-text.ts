export const PROJECT_ROOT_COMMAND_HELP =
	"Run this command from the root of the target project.";

export const PROJECT_ROOT_PROGRAM_HELP = [
	"Project commands (`init`, `index`, `search`, `structure`, `architecture`, `explain`, `deps`, `uninstall`) must be run from the root of the target project.",
	"`setup` and `reinit` can be run from anywhere.",
].join("\n");
